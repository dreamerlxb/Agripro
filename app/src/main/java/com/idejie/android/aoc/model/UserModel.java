package com.idejie.android.aoc.model;

import com.strongloop.android.loopback.Model;
import com.strongloop.android.loopback.User;

/**
 * Created by shandongdaxue on 16/9/2.
 */
public class UserModel extends User{
    /**
     * name (string, optional),
     isBackEndUser (boolean, optional),
     zoneCode (string, optional),
     mobileNumber (string, optional),
     mobileNumberVerified (boolean, optional),
     score (object, optional),
     realm (string, optional),
     username (string, optional),
     credentials (object, optional),
     challenges (object, optional),
     email (string),
     emailVerified (boolean, optional),
     status (string, optional),
     created (string, optional),
     lastUpdated (string, optional),
     id (number, optional),
     imageId (number, optional)
     */

}
